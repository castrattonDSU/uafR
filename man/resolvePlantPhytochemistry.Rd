% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plantPhytochemistry.R
\name{resolvePlantPhytochemistry}
\alias{resolvePlantPhytochemistry}
\title{Species-first plant phytochemistry resolver}
\usage{
resolvePlantPhytochemistry(
  plants,
  sources = c("lotus", "knapsack", "npass", "pubchem", "pubmed", "pubtator"),
  taxon_fallback = c("species", "genus"),
  enrich_compounds = TRUE,
  chemical_library = NULL,
  detail = c("research", "full", "none"),
  cache = TRUE,
  cache_dir = NULL,
  throttle = 0.2,
  ncbi_email = Sys.getenv("NCBI_EMAIL", ""),
  ncbi_tool = Sys.getenv("NCBI_TOOL", "uafR"),
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", ""),
  max_pubmed_records = 50,
  max_provider_records = max_pubmed_records,
  min_confidence = "medium",
  enrichment_batch_size = Inf,
  resume_enrichment = TRUE,
  progress = interactive(),
  strict = FALSE,
  curated_data = NULL,
  provider_results = NULL,
  enrichment_fun = NULL,
  request_fun = NULL,
  pubtator_request_fun = NULL,
  refresh = FALSE,
  ...
)
}
\arguments{
\item{plants}{Character vector or data frame of plant names. If a data frame
is supplied, a `species` column is preferred; otherwise the first column is
treated as the plant name. Optional `genus` and `family` columns are used
when present.}

\item{sources}{Character vector of provider names. Supported names are
`"lotus"`, `"knapsack"`, `"npass"`, `"pubchem"`, `"pubmed"`, and
`"pubtator"`.}

\item{taxon_fallback}{Character vector of fallback ranks to record in query
planning. Species-level evidence is queried first. Providers that support
broader lookup may also use genus or family terms; fallback evidence is
labeled separately from direct species evidence.}

\item{enrich_compounds}{Logical. If `TRUE`, attempt compound enrichment after
occurrence discovery.}

\item{chemical_library}{Optional chemical library passed to `categorate()`
when compound enrichment is requested. If omitted, compound enrichment falls
back to `pubchemProfile()` and returns PubChem-derived categorate-like
analysis tables without FMCS library matching.}

\item{detail}{One of `"research"`, `"full"`, or `"none"`.}

\item{cache}{Logical. If `TRUE`, provider requests may use cache files.}

\item{cache_dir}{Cache directory. Defaults to a uafR cache directory when
needed.}

\item{throttle}{Seconds to wait between uncached provider requests.}

\item{ncbi_email}{Optional NCBI email. Defaults to `Sys.getenv("NCBI_EMAIL")`.}

\item{ncbi_tool}{Optional NCBI tool name. Defaults to `Sys.getenv("NCBI_TOOL",
"uafR")`.}

\item{ncbi_api_key}{Optional NCBI API key. Defaults to
`Sys.getenv("NCBI_API_KEY")`. It is used in request URLs but is not persisted
in result tables.}

\item{max_pubmed_records}{Maximum PubMed records per plant to request.}

\item{max_provider_records}{Maximum occurrence rows to retain per plant from
non-PubMed provider adapters.}

\item{min_confidence}{Minimum confidence for summary/matrix features.}

\item{enrichment_batch_size}{Maximum number of unique compounds per
PubChem-only enrichment batch. Use `Inf` for one batch.}

\item{resume_enrichment}{Logical. If `TRUE`, reuse saved PubChem-only
enrichment batch files when available.}

\item{progress}{Logical. If `TRUE`, print simple enrichment progress
messages.}

\item{strict}{Logical. If `TRUE`, validation treats optional missing fields
more strictly.}

\item{curated_data}{Optional local species-compound table to standardize and
include.}

\item{provider_results}{Optional named list of mocked or pre-fetched provider
results. This is intended for tests, offline workflows, and future cached
provider download parsers.}

\item{enrichment_fun}{Optional function used instead of `categorate()` for
compound enrichment. It should accept a character vector of compounds and
return a categorate-like list.}

\item{request_fun}{Optional URL request function used by live-capable
provider adapters in tests or advanced use.}

\item{pubtator_request_fun}{Optional URL request function used by the
PubTator adapter in tests or advanced use.}

\item{refresh}{Logical. Reserved for future provider cache refresh support.}

\item{...}{Additional arguments passed to `categorate()`, `enrichment_fun`,
or the PubChem-only enrichment fallback. Use `compound_request_fun` in `...`
to mock or customize compound-centered PubChem requests.}
}
\value{
A list with class `"uaf_plant_phytochemistry"` containing normalized
query, occurrence, literature, enrichment, summary, matrix, validation,
dictionary, and provenance tables.
}
\description{
`resolvePlantPhytochemistry()` builds a species-first phytochemistry result
from plant names, public-provider adapters, literature candidates, and
optional curated species-compound rows. The resolver is intentionally
conservative: no compound is fabricated, literature co-mentions remain
candidate evidence, and provider no-hit states are reported through
diagnostics.
}
\examples{
\dontrun{
plants = c("Salix nigra", "Camellia sinensis", "Zea mays")
phyto = resolvePlantPhytochemistry(
  plants = plants,
  sources = c("lotus", "pubmed", "pubtator"),
  taxon_fallback = c("species", "genus"),
  enrich_compounds = FALSE
)
phyto$SpeciesChemistrySummary
validatePlantPhytochemistryResult(phyto)$Summary
}

}
