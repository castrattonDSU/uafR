% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plantPhytochemistry.R
\name{runPlantPhytochemistryPilot}
\alias{runPlantPhytochemistryPilot}
\title{Run a plant phytochemistry pilot workflow}
\usage{
runPlantPhytochemistryPilot(
  plants,
  out_dir,
  sources = c("lotus", "knapsack", "npass", "pubchem", "pubmed", "pubtator"),
  taxon_fallback = c("species", "genus"),
  cache_dir = NULL,
  species_chunk_size = 25,
  compound_resolution_profile = c("identity", "none", "research", "full"),
  occurrence_status = c("direct_reported", "curated_reported"),
  analysis_ready = TRUE,
  min_confidence = "medium",
  max_compounds_per_species = Inf,
  max_unique_compounds = Inf,
  chemical_library = NULL,
  cache = TRUE,
  throttle = 0.5,
  ncbi_email = Sys.getenv("NCBI_EMAIL", ""),
  ncbi_tool = Sys.getenv("NCBI_TOOL", "uafR"),
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", ""),
  max_pubmed_records = 25,
  max_provider_records = 100,
  lotus_index = Sys.getenv("UAFR_LOTUS_INDEX", ""),
  request_timeout = 30,
  provider_results = NULL,
  enrichment_fun = NULL,
  pubchem_fun = NULL,
  request_fun = NULL,
  pubtator_request_fun = NULL,
  compound_request_fun = NULL,
  compound_batch_size = 50,
  matrix_feature = c("comparison_group", "biosynthetic_family", "chemical_behavior",
    "compound", "comparison_scope"),
  matrix_mode = c("binary", "count", "confidence"),
  max_matrix_features = Inf,
  resume = TRUE,
  progress = interactive(),
  overwrite = FALSE,
  strict = FALSE,
  stop_on_error = FALSE,
  refresh = FALSE,
  ...
)
}
\arguments{
\item{plants}{Character vector or data frame of plant names.}

\item{out_dir}{Output directory for audit tables, pilot reports, and
matrices.}

\item{sources}{Provider sources passed to `runPlantPhytochemistryBatch()`.}

\item{taxon_fallback}{Taxon fallback ranks passed to
`runPlantPhytochemistryBatch()`.}

\item{cache_dir}{Cache directory. Defaults to `file.path(out_dir, "cache")`.}

\item{species_chunk_size}{Number of species per discovery chunk.}

\item{compound_resolution_profile}{One of `"identity"`, `"none"`,
`"research"`, or `"full"`.}

\item{occurrence_status}{Occurrence statuses retained for compound
resolution.}

\item{analysis_ready}{Logical filter for compound-resolution input.}

\item{min_confidence}{Minimum confidence for resolution and matrices.}

\item{max_compounds_per_species}{Maximum analysis-ready compounds resolved
per species.}

\item{max_unique_compounds}{Maximum unique compounds resolved across the
pilot.}

\item{chemical_library}{Optional library passed to rich enrichment.}

\item{cache}{Logical. If `TRUE`, use cache/checkpoint files.}

\item{throttle}{Seconds to wait between uncached provider requests.}

\item{ncbi_email}{Optional NCBI email.}

\item{ncbi_tool}{Optional NCBI tool name.}

\item{ncbi_api_key}{Optional NCBI API key. It is not written to output
files.}

\item{max_pubmed_records}{Maximum PubMed records per plant.}

\item{max_provider_records}{Maximum non-PubMed provider records per plant.}

\item{lotus_index}{Optional local LOTUS index as a data frame, flat file
path, or manifest-backed lookup directory. Passed to
`runPlantPhytochemistryBatch()` and used instead of the live LOTUS simple API
when `"lotus"` is enabled.}

\item{request_timeout}{Maximum seconds allowed for an uncached provider
request.}

\item{provider_results}{Optional mocked or pre-fetched provider results.}

\item{enrichment_fun}{Optional rich enrichment function for tests or cached
workflows.}

\item{pubchem_fun}{Optional replacement for `pubchemProfile()`.}

\item{request_fun}{Optional provider request function.}

\item{pubtator_request_fun}{Optional PubTator request function.}

\item{compound_request_fun}{Optional request function for compound
resolution.}

\item{compound_batch_size}{Maximum compounds per compound-resolution batch.}

\item{matrix_feature}{Matrix feature family passed to
`plantComparableChemistryMatrix()`.}

\item{matrix_mode}{Matrix mode passed to `plantComparableChemistryMatrix()`.}

\item{max_matrix_features}{Maximum feature columns retained per pilot
matrix.}

\item{resume}{Logical. If `TRUE`, reuse checkpoints where available.}

\item{progress}{Logical. If `TRUE`, print progress messages.}

\item{overwrite}{Logical. If `TRUE`, replace existing output files.}

\item{strict}{Logical passed to validation.}

\item{stop_on_error}{Logical. If `TRUE`, stop on first failed chunk.}

\item{refresh}{Logical passed to provider adapters.}

\item{...}{Additional arguments passed to rich enrichment.}
}
\value{
A `uaf_plant_phytochemistry` result with `PilotSummary`,
`PilotReviewNeeded`, `PilotMatrices`, and `PilotExportManifest` tables.
}
\description{
`runPlantPhytochemistryPilot()` is a higher-level workflow for small live
provider pilots before scaling to hundreds of species. It runs
`runPlantPhytochemistryBatch()`, writes the full audit bundle, and adds
pilot-specific files that make provider coverage, review needs, biological
context, and comparable-chemistry matrices easy to inspect.

The pilot does not claim complete plant metabolomes. It separates direct
species evidence, fallback/candidate evidence, source-backed biological
context, and chemistry-comparison scope so users can decide which records are
suitable for downstream analysis.
}
