% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pubchemProfile.R
\name{pubchemProfile}
\alias{pubchemProfile}
\title{Build a detailed PubChem profile for query chemicals}
\usage{
pubchemProfile(
  compounds,
  profile = c("minimal", "ms", "safety", "bioactivity", "full"),
  sections = NULL,
  sources = NULL,
  cache = TRUE,
  cache_dir = NULL,
  throttle = 0.2,
  assay_detail_limit = 50,
  request_fun = NULL
)
}
\arguments{
\item{compounds}{A character vector of chemical names to resolve on PubChem.}

\item{profile}{One of \code{"minimal"}, \code{"ms"}, \code{"safety"},
\code{"bioactivity"}, or \code{"full"}. Profiles control which PubChem
properties and PUG-View headings are requested.}

\item{sections}{Optional PUG-View headings to request in addition to the
selected profile.}

\item{sources}{Optional PUG-View source names to request in addition to the
selected profile. Use this for source-specific records such as LOTUS, FEMA,
FDA/SPL, or MeSH.}

\item{cache}{Logical. If \code{TRUE}, raw PubChem JSON responses are cached
under \code{cache_dir}.}

\item{cache_dir}{Directory for cached PubChem responses. Defaults to a
user-cache location from \code{tools::R_user_dir()} when available, otherwise a
temporary directory.}

\item{throttle}{Seconds to wait between uncached PubChem requests.}

\item{assay_detail_limit}{Maximum number of unique PubChem BioAssay AIDs per
query for which assay-description metadata is fetched when \code{profile} is
\code{"bioactivity"} or \code{"full"}. Active, numeric, and target-bearing
assays are prioritized. Set to \code{0} to skip assay-description requests.}

\item{request_fun}{Optional function used to retrieve a URL. This is intended
for tests and advanced users. It should return either JSON text or a parsed
list.}
}
\value{
A list with tidy data frames: \code{identity}, \code{properties},
\code{synonyms}, \code{annotations}, \code{source_annotations},
\code{taxonomy}, \code{classifications}, \code{spectra}, \code{safety},
\code{experimental}, \code{bioactivity}, \code{bioassay_details}, and
\code{provenance}.
Annotation-derived tables include raw \code{Value}, normalized
\code{CleanValue}, parsed \code{ValueNumeric}, and normalized \code{UnitClean}
columns when those fields can be extracted. The \code{bioactivity} table stores
one PubChem BioAssay summary row per assay with activity outcome, assay
name/type, target identifiers, and numeric activity values when PubChem reports
them. The \code{bioassay_details} table stores bounded assay-description
metadata for selected AIDs. The returned object has class
\code{"uaf_pubchem_profile"}.
}
\description{
\code{pubchemProfile()} retrieves structured PubChem information for query
chemicals and returns tidy tables that can be reused by downstream uafR
workflows. It separates PubChem enrichment from mass spectrometry processing
so results can be cached, inspected, and tested independently.
}
\examples{
\dontrun{
profile = pubchemProfile(c("methyl salicylate", "octanal"), profile = "ms")
profile$properties
profile$spectra
}
}
