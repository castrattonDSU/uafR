% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plantPhytochemistry.R
\name{enrichPlantCompounds}
\alias{enrichPlantCompounds}
\title{Enrich plant compounds with existing uafR compound workflows}
\usage{
enrichPlantCompounds(
  plant_compounds,
  chemical_library = NULL,
  detail = c("research", "full", "none"),
  cache = TRUE,
  cache_dir = NULL,
  throttle = 0.2,
  enrichment_fun = NULL,
  pubchem_fun = NULL,
  compound_request_fun = NULL,
  batch_size = Inf,
  resume = TRUE,
  progress = interactive(),
  ...
)
}
\arguments{
\item{plant_compounds}{A normalized or curated plant-compound table.}

\item{chemical_library}{Optional library passed to `categorate()`.}

\item{detail}{One of `"research"`, `"full"`, or `"none"`.}

\item{cache}{Logical.}

\item{cache_dir}{Cache directory.}

\item{throttle}{Request throttle.}

\item{enrichment_fun}{Optional injected enrichment function for tests or
cached workflows.}

\item{pubchem_fun}{Optional replacement for `pubchemProfile()` used by the
PubChem-only fallback. Intended for tests and advanced cached workflows.}

\item{compound_request_fun}{Optional request function passed to
`pubchemProfile()` when the PubChem-only fallback is used.}

\item{batch_size}{Maximum number of unique compounds per PubChem-only
enrichment batch. Use `Inf` for one batch.}

\item{resume}{Logical. If `TRUE`, reuse saved PubChem-only batch `.rds`
files in `cache_dir`.}

\item{progress}{Logical. If `TRUE`, print simple progress messages during
PubChem-only enrichment.}

\item{...}{Additional arguments passed to `categorate()`, `enrichment_fun`,
or the PubChem-only enrichment fallback.}
}
\value{
A list with `CategorateResult`, `CompoundResolution`,
`TraitEvidence`, and `Provenance`.
}
\description{
Enrich plant compounds with existing uafR compound workflows
}
