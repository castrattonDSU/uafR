% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plantPhytochemistry.R
\name{runPlantPhytochemistryBatch}
\alias{runPlantPhytochemistryBatch}
\title{Run a staged plant phytochemistry batch workflow}
\usage{
runPlantPhytochemistryBatch(
  plants,
  sources = c("lotus", "knapsack", "npass", "pubchem", "pubmed", "pubtator"),
  taxon_fallback = c("species", "genus"),
  out_dir = NULL,
  cache_dir = NULL,
  species_chunk_size = 25,
  compound_resolution_profile = c("identity", "none", "research", "full"),
  occurrence_status = c("direct_reported", "curated_reported"),
  analysis_ready = TRUE,
  min_confidence = "medium",
  max_compounds_per_species = Inf,
  max_unique_compounds = Inf,
  chemical_library = NULL,
  cache = TRUE,
  throttle = 0.2,
  ncbi_email = Sys.getenv("NCBI_EMAIL", ""),
  ncbi_tool = Sys.getenv("NCBI_TOOL", "uafR"),
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", ""),
  max_pubmed_records = 50,
  max_provider_records = max_pubmed_records,
  lotus_index = Sys.getenv("UAFR_LOTUS_INDEX", ""),
  request_timeout = 30,
  provider_results = NULL,
  enrichment_fun = NULL,
  pubchem_fun = NULL,
  request_fun = NULL,
  pubtator_request_fun = NULL,
  compound_request_fun = NULL,
  compound_batch_size = 100,
  resume = TRUE,
  progress = interactive(),
  overwrite = FALSE,
  strict = FALSE,
  stop_on_error = FALSE,
  refresh = FALSE,
  ...
)
}
\arguments{
\item{plants}{Character vector or data frame of plant names.}

\item{sources}{Public provider names passed to `resolvePlantPhytochemistry()`.}

\item{taxon_fallback}{Fallback ranks passed to `resolvePlantPhytochemistry()`.}

\item{out_dir}{Optional output directory for checkpoints and CSV/JSON
products.}

\item{cache_dir}{Cache directory. If omitted and `out_dir` is supplied, a
`cache` subdirectory under `out_dir` is used.}

\item{species_chunk_size}{Number of species per discovery chunk.}

\item{compound_resolution_profile}{One of `"identity"`, `"none"`,
`"research"`, or `"full"`. `"identity"` performs a fast PubChem identity
pass; `"research"` and `"full"` run richer uafR compound enrichment on the
filtered resolution set.}

\item{occurrence_status}{Occurrence statuses used to select compounds for
resolution. Defaults to direct and curated reported records.}

\item{analysis_ready}{Optional logical used to select compounds for
resolution.}

\item{min_confidence}{Minimum confidence used to select compounds for
resolution.}

\item{max_compounds_per_species}{Optional cap on unique compounds selected
per species for compound resolution.}

\item{max_unique_compounds}{Optional cap on total unique compounds selected
for compound resolution.}

\item{chemical_library}{Optional library passed to rich compound enrichment.}

\item{cache}{Logical. If `TRUE`, provider requests and batch checkpoints are
reused where possible.}

\item{throttle}{Seconds to wait between uncached provider requests.}

\item{ncbi_email}{Optional NCBI email.}

\item{ncbi_tool}{Optional NCBI tool name.}

\item{ncbi_api_key}{Optional NCBI API key.}

\item{max_pubmed_records}{Maximum PubMed records per plant.}

\item{max_provider_records}{Maximum occurrence rows retained per plant from
non-PubMed providers.}

\item{lotus_index}{Optional local LOTUS index as a data frame, flat file
path, or manifest-backed lookup directory. Passed to
`resolvePlantPhytochemistry()` and used instead of the live LOTUS simple API
when `"lotus"` is enabled.}

\item{request_timeout}{Maximum seconds allowed for an uncached provider
request.}

\item{provider_results}{Optional mocked or pre-fetched provider results.}

\item{enrichment_fun}{Optional rich enrichment function for tests or cached
workflows.}

\item{pubchem_fun}{Optional replacement for `pubchemProfile()`.}

\item{request_fun}{Optional provider request function.}

\item{pubtator_request_fun}{Optional PubTator request function.}

\item{compound_request_fun}{Optional request function passed to
`pubchemProfile()` during compound resolution.}

\item{compound_batch_size}{Maximum number of unique compounds per compound
resolution batch.}

\item{resume}{Logical. If `TRUE`, reuse discovery and compound checkpoints.}

\item{progress}{Logical. If `TRUE`, print simple progress messages.}

\item{overwrite}{Logical. If `TRUE`, replace existing files in `out_dir`.}

\item{strict}{Logical passed to validation.}

\item{stop_on_error}{Logical. If `TRUE`, stop on the first failed discovery
chunk; otherwise record the failed chunk and continue.}

\item{refresh}{Logical passed to provider adapters.}

\item{...}{Additional arguments passed to rich compound enrichment.}
}
\value{
A `uaf_plant_phytochemistry` result with additional
`BatchRunManifest` and `BatchChunkManifest` tables.
}
\description{
Runs species-first discovery in resumable species chunks, then optionally
resolves compounds through a staged identity or enrichment pass. The default
compound stage is `"identity"` because live provider pilots show that broad
rich PubChem enrichment is the production bottleneck for hundreds of plant
names.
}
