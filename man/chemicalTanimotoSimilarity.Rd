% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chemicalTanimoto.R
\name{chemicalTanimotoSimilarity}
\alias{chemicalTanimotoSimilarity}
\title{Pairwise PubChem fingerprint Tanimoto similarity for chemicals}
\usage{
chemicalTanimotoSimilarity(
  compounds,
  compound_col = NULL,
  cid_col = NULL,
  inchikey_col = NULL,
  smiles_col = NULL,
  compound_id_col = NULL,
  group_cols = NULL,
  thresholds = c(0.5, 0.7, 0.85, 0.95),
  top_n_pairs = 5,
  return_compound_pairs = TRUE,
  return_group_compound_pairs = FALSE,
  out_dir = NULL,
  max_in_memory_pairs = 1e+06,
  pair_block_size = 250,
  cache = TRUE,
  cache_dir = NULL,
  throttle = 0.2,
  refresh = FALSE,
  request_fun = NULL,
  name_fallback = TRUE
)
}
\arguments{
\item{compounds}{Character vector or data frame. Character vectors are
treated as compound names. Data frames can contain compound names, PubChem
CIDs, InChIKeys, SMILES, and optional grouping columns.}

\item{compound_col}{Column containing compound names when `compounds` is a
data frame. If `NULL`, common names such as `compound_name`, `Query`, or
`Chemical` are detected.}

\item{cid_col}{Column containing PubChem CIDs. If `NULL`, common names such
as `CID`, `pubchem_cid`, or `cid` are detected.}

\item{inchikey_col}{Column containing InChIKeys. If `NULL`, common names such
as `InChIKey` or `inchikey` are detected.}

\item{smiles_col}{Optional SMILES column retained as metadata. PubChem
Fingerprint2D values are still fetched from PubChem so the Tanimoto
definition is consistent across workflows.}

\item{compound_id_col}{Optional stable compound identifier column. If absent,
identifiers are derived from InChIKey, PubChem CID, or compound name.}

\item{group_cols}{Optional grouping columns. For plants, use `"species"` or
call `plantChemicalTanimotoSimilarity()`.}

\item{thresholds}{Numeric Tanimoto thresholds to count in group summaries.}

\item{top_n_pairs}{Number of top compound-pair explanations to include in
group summaries.}

\item{return_compound_pairs}{Logical. If `TRUE`, include or write the
compound-compound pair table.}

\item{return_group_compound_pairs}{Logical. If `TRUE` and `group_cols` are
supplied, include or write all cross-group compound-pair rows.}

\item{out_dir}{Optional output directory. When supplied, large pairwise
tables are written as compressed CSV files and the result records their
paths.}

\item{max_in_memory_pairs}{Maximum pair rows allowed in memory when
`out_dir` is `NULL`.}

\item{pair_block_size}{Number of focal rows per pairwise computation block.}

\item{cache}{Logical. If `TRUE`, PubChem JSON responses are cached.}

\item{cache_dir}{Cache directory. Defaults to the uafR user cache.}

\item{throttle}{Seconds to wait after uncached PubChem requests.}

\item{refresh}{Logical. If `TRUE`, ignore cached PubChem responses.}

\item{request_fun}{Optional request function for tests. It receives a URL and
returns parsed JSON or JSON text.}

\item{name_fallback}{Logical. If `TRUE`, unresolved compounds may be queried
by compound name after CID and InChIKey attempts fail.}
}
\value{
A list with class `"uaf_tanimoto_similarity"` containing
`CompoundInput`, `CompoundResolution`, `PubChemFingerprints`,
`CompoundTanimoto`, `GroupCompoundMembership`,
`GroupPairTanimotoSummary`, `GroupCompoundTanimoto`, `ExportManifest`, and
`Provenance`.
}
\description{
`chemicalTanimotoSimilarity()` resolves chemicals to PubChem CIDs, fetches
PubChem Fingerprint2D bit strings, decodes them with `ChemmineR`, and
computes pairwise Tanimoto similarity. It accepts a plain character vector or
a data frame with compound identifiers. When a grouping column is supplied,
it also returns group-level summaries that are suitable for joining to other
sample, species, treatment, or site similarity tables.
}
\examples{
\dontrun{
compounds = data.frame(
  species = c("Plant A", "Plant A", "Plant B"),
  compound_name = c("caffeine", "theobromine", "aspirin")
)
sim = chemicalTanimotoSimilarity(compounds, group_cols = "species")
sim$GroupPairTanimotoSummary
}

}
