% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorateExport.R
\name{exportCategorateWorkbook}
\alias{exportCategorateWorkbook}
\title{Export enriched categorate results for review and sharing}
\usage{
exportCategorateWorkbook(
  x,
  path,
  tables = NULL,
  format = c("auto", "xlsx", "csv"),
  include_raw = FALSE,
  include_empty = TRUE,
  overwrite = FALSE,
  max_cell_chars = 30000
)
}
\arguments{
\item{x}{A list returned by \code{categorate()}, preferably with
\code{detail = "research"} or \code{detail = "full"}.}

\item{path}{Output path. For \code{format = "csv"}, this is a directory. For
\code{format = "xlsx"}, this is an \code{.xlsx} file.}

\item{tables}{Optional character vector of result table names to export. If
\code{NULL}, a curated analysis-ready set is exported.}

\item{format}{Export format: \code{"csv"}, \code{"xlsx"}, or \code{"auto"}.
\code{"auto"} writes \code{.xlsx} when \code{path} ends in \code{.xlsx} and an
Excel writer is installed; otherwise it writes a CSV bundle.}

\item{include_raw}{Logical. If \code{TRUE} and \code{tables = NULL}, export
every data frame in \code{x}, including raw source tables. If \code{FALSE},
export the curated analysis and diagnostics tables.}

\item{include_empty}{Logical. If \code{TRUE}, include empty data frames so the
bundle preserves expected schema. If \code{FALSE}, omit empty tables.}

\item{overwrite}{Logical. If \code{TRUE}, replace an existing output file or
directory.}

\item{max_cell_chars}{Maximum characters retained in a single cell. Longer
values are truncated to keep spreadsheets responsive.}
}
\value{
A manifest data frame describing exported tables, row/column counts, sheet
names, file names, and output path.
}
\description{
\code{exportCategorateWorkbook()} writes the most useful \code{categorate()}
result tables to a researcher-friendly export bundle. It always supports a
directory of CSV files with an \code{ExportManifest} table. If \code{openxlsx}
or \code{writexl} is installed, it can also write a multi-sheet \code{.xlsx}
workbook.
}
\examples{
\dontrun{
result = categorate(compounds, library_data, detail = "research")
manifest = exportCategorateWorkbook(result, "categorate_export",
                                    format = "csv", overwrite = TRUE)
manifest
}
}
