# uafR workflow guide

Returns a compact decision table that maps common research situations to
the recommended uafR workflow. Use this as the first stop when deciding
which function family to run.

## Usage

``` r
uafRWorkflowGuide()
```

## Value

A data frame with workflow recommendations and stability labels.
