# Summarize a uafR cache directory

Summarize a uafR cache directory

## Usage

``` r
summarizeUafRCache(cache_dir, recursive = TRUE)
```

## Arguments

- cache_dir:

  Cache directory.

- recursive:

  Logical. If \`TRUE\`, inspect nested files.

## Value

Provider-level cache summary.
