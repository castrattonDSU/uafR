# uafR provider contracts

Returns a source-contract table describing how uafR interprets public
data providers. The table is documentation and audit metadata; it does
not query any live services.

## Usage

``` r
uafRProviderContracts(providers = NULL)
```

## Arguments

- providers:

  Optional providers to return.

## Value

Data frame with provider contract records.
