# Estimate Tanimoto output size

Estimate Tanimoto output size

## Usage

``` r
estimateTanimotoOutput(compound_count, membership_count = NULL)
```

## Arguments

- compound_count:

  Number of unique structure-resolved compounds.

- membership_count:

  Optional plant/group-compound membership rows.

## Value

Data frame with pair-count and approximate CSV-size estimates.
