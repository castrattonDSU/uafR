# uafR API stability table

Returns the current public API stability classification for core
user-facing functions. Stability labels are documentation commitments
for downstream scripts and training material.

## Usage

``` r
uafRApiStability()
```

## Value

A data frame with function names, workflow areas, and stability.
